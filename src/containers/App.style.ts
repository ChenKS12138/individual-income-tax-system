import { createStyle } from "~/utils";

const styles = {
  app: createStyle({
    padding: "20px",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  }),
};

export default styles;
